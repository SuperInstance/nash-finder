[package]
name = "nash-finder"
version = "0.1.0"
edition = "2024"
description = "Nash equilibrium computation for agent strategic interactions"
license = "MIT"
repository = "https://github.com/SuperInstance/nash-finder"
keywords = ["game-theory", "nash-equilibrium", "agent", "strategic-reasoning"]
categories = ["mathematics", "science"]

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
